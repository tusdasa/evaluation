server:
  port: 7052
  servlet:
    context-path: /teacherevaluation
spring:
  application:
    name: evaluation-teacher-evaluation
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://tusdasa:8000/eureka/
feign:
  hystrix:
    enabled: false
  okhttp:
    enabled: true